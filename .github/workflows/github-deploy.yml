name: GitHub Deploy
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  Deploy:
    runs-on: self-hosted
    environment: production
    steps:
      - name: check runner
        run: whoami